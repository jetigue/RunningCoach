
@if($track400mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track400mResults2015 }}">
        400m
    </athlete-track-results>
@endif
@if($track800mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track800mResults2015 }}">
        800m
    </athlete-track-results>
@endif
@if($track1000mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track1000mResults2015 }}">
        1000m
    </athlete-track-results>
@endif
@if($track1200mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track1200mResults2015 }}">
        1200m
    </athlete-track-results>
@endif
@if($track1500mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track1500mResults2015 }}">
        1500m
    </athlete-track-results>
@endif
@if($track1600mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track1600mResults2015 }}">
        1600m
    </athlete-track-results>
@endif
@if($track1mileResults2015->count() > 0)
    <athlete-track-results :data="{{ $track1mileResults2015 }}">
        1 Mile
    </athlete-track-results>
@endif
@if($track3000mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track3000mResults2015 }}">
        3000m
    </athlete-track-results>
@endif
@if($track3200mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track3200mResults2015 }}">
        3200m
    </athlete-track-results>
@endif
@if($track2mileResults2015->count() > 0)
    <athlete-track-results :data="{{ $track2mileResults2015 }}">
        2 Mile
    </athlete-track-results>
@endif
@if($track5000mResults2015->count() > 0)
    <athlete-track-results :data="{{ $track5000mResults2015 }}">
        5000m
    </athlete-track-results>
@endif
